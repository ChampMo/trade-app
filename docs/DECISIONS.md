# DECISIONS — การตัดสินใจที่ล็อกแล้ว

แก้ไฟล์นี้ก่อนแก้โค้ดเสมอ ทุกข้อมีวันที่และเหตุผล ถ้าจะกลับคำต้องเขียนข้อใหม่ ไม่ลบข้อเดิม

| # | วันที่ | การตัดสินใจ | เหตุผล / ผลต่อโค้ด |
|---|---|---|---|
| D1 | 2026-09-02 | โบรก **XM** บน **MT5** เท่านั้น ไม่รองรับ MT4 | MT5 มี Python package ทางการ · MT4 ต้องเขียน EA เชื่อมผ่านไฟล์ซึ่งเปราะ |
| D2 | 2026-09-02 | เริ่มที่ **EURUSD** คู่เดียว | spread ต่ำ พฤติกรรมนิ่ง เหมาะพิสูจน์สถาปัตยกรรมก่อนหา edge |
| D3 | 2026-09-02 | รับ **drawdown สูงสุด 30%** → kill switch ที่ 30% · daily loss limit 3% · risk ต่อไม้เดือนแรกของเงินจริง 0.25% · max open risk 1% · max positions 3 | ตัวเลขนี้เป็น input ของ Risk Engine ไม่ใช่ผลลัพธ์ · gate ก่อนเงินจริง: forward demo ≥ 90 วันไม่แตะพารามิเตอร์ · ≥ 200 ไม้ · DD จริงบน demo ≤ 15% · drill kill switch 3/3 · ผ่านข่าวแรง ≥ 3 ครั้ง · slippage วัดตอน live_small |
| D4 | 2026-09-02 | **ไม่ขาย** แอปให้คนอื่น | ไม่ต้องทำ multi-account, license, แยก config ผู้ใช้ |
| D5 | 2026-09-02 | **Build-time AI ≠ Run-time AI** · Claude Code ใช้พัฒนาเท่านั้น · DeepSeek คือ AI ตอนรัน | บังคับด้วย `tests/test_no_claude_dependency.py` |
| D6 | 2026-09-02 | AI แก้ได้แค่ **bias / size_mult / block** และทุกค่ามีวันหมดอายุ · หัวหน้าเป็นโค้ด ไม่ใช่ LLM · ไม่มี agent อ่านกราฟ | LLM ไม่ deterministic ถ้าให้สั่งเทรดจะ backtest ไม่ได้ · LLM อ่านแท่งเทียนเป็น noise ที่พูดมั่นใจ |
| D7 | 2026-09-02 | UI คุยกับ core ผ่าน **localhost HTTP + WebSocket** ไม่ import กันตรง · core รันเป็น background process โดยไม่ต้องเปิด UI | ปิดหน้าต่างแล้วบอทเทรดต่อ · ย้าย VPS หรือเปลี่ยน shell ได้โดยไม่แตะ logic |
| D8 | 2026-09-02 | **โปรไฟล์การเชื่อมต่อ** = core 1 ตัว + MT5 terminal 1 ชุด + โหมด 1 แบบ · Demo (port 8001, `C:\Program Files\MetaTrader 5`) และ Live (port 8002, ติดตั้งแยกภายหลัง) · สีของ UI เปลี่ยนตามโหมด | แยกเงินจริงออกจากเครื่องมือพัฒนาทางกายภาพ · `ALLOW_LIVE=true` มีเฉพาะบนโปรไฟล์ Live |
| D9 | 2026-09-02 | กลยุทธ์มี **variant A/B/C** (rules · +calendar · +AI) เป็น magic number คนละตัว รันคู่กันบนฟีดเดียว | ตอบได้ด้วยตัวเลขว่า AI ช่วยจริงไหม · ถ้า C ไม่ชนะ B หลัง 3 เดือน ถอด AI |
| D10 | 2026-09-02 | **Strategy lifecycle** research → backtested → forward → live_small → live → retired · gate ระหว่างสถานะเป็นตัวเลขที่แอปตรวจ | วินัยอยู่ในซอฟต์แวร์ ไม่ใช่ในใจตอนตื่นเต้น |
| D11 | 2026-09-02 | Post-mortem ให้ผลลัพธ์เป็น **รายงาน** · ข้อเสนอแก้พารามิเตอร์เป็นข้อยกเว้น ต้องมีหลักฐาน ≥ 30 ไม้ + backtest เต็มประวัติ + walk-forward และเปลี่ยนตามรอบตายตัว (เดือนละครั้ง) · การขาดทุนจัดหมวด variance / execution / regime / bug · มีแค่ execution กับ bug ที่นำไปสู่การแก้โค้ด | กัน overfit กับสัปดาห์ที่แล้ว · แพ้ติดกัน 5 ไม้เป็นเรื่องปกติของ win rate 45% |
| D12 | 2026-09-02 | Kill switch เป็นโค้ดล้วน · ปลดล็อกต้องใส่เหตุผล · หลังปลดล็อกเป็น **PAUSED** ไม่ใช่ RUNNING · Telegram `/kill` ให้ผลเหมือนปุ่ม | ไม่มี AI และไม่มี Claude ในเส้นทางฉุกเฉิน |
| D12a | 2026-09-03 | Trigger ของ kill switch มี **7 ข้อ** คือ daily loss 3% · drawdown 30% · MT5 เงียบเกิน 60 วิ · order ถูกปฏิเสธติดกัน 3 ครั้ง · reconcile ไม่ตรง · **โพซิชันไม่มี SL ที่โบรก** · manual · ข้อที่เพิ่มจากแผนเดิมคือข้อโพซิชันไม่มี SL | โพซิชันที่ไม่มี stop คือความเสี่ยงไม่จำกัด (ละเมิดกฎ 03) จึงเป็นเหตุฉุกเฉิน ไม่ใช่แค่คำเตือน · kill ที่ปิดไม่หมดต้องรายงาน `complete=False` เสมอ ห้ามอ้างว่าสำเร็จ · ถ้าอ่านโพซิชันจากโบรกไม่ได้เลย ต้องบอกว่า "ไม่รู้" ไม่ใช่ "0" |
| D13 | 2026-09-02 | เวลาใน journal เป็น **UTC** ทั้งหมด (naive UTC ใน SQLite) · เก็บ offset ของ server โบรกไว้ด้วย · UI แสดงเวลาไทยได้แต่ตารางคง UTC | MT5 ใช้เวลา server ไม่ใช่ UTC ไม่ใช่เวลาไทย · วัดจริง 2026-09-02: MetaQuotes-Demo เดิน **UTC+3** โบรกโชว์ 19:47 ขณะที่ UTC จริงคือ 16:47 · ถ้าไม่แปลง ทุกแถวใน journal จะเพี้ยน 3 ชม. เงียบ ๆ · วิธีวัดอยู่ใน `broker/servertime.py` เรียกซ้ำได้หลัง reconnect และช่วงเปลี่ยน DST (ปีละ 2 ครั้ง โบรก EET ขยับระหว่าง +2 กับ +3) |
| D14 | 2026-09-02 | Python 3.13 (package MetaTrader5 มี wheel cp313) · `requires-python >= 3.11` · SQLAlchemy 2 + Pydantic 2 · pydantic-settings อ่าน `.env` | ตรงกับ stack ที่ตกลงกัน |
| D15 | 2026-09-02 | ป้ายใน UI เป็นภาษาอังกฤษ · โค้ดและ commit เป็นอังกฤษ · เอกสารสำหรับเจ้าของเป็นไทยได้ | ศัพท์เทรดเป็นสากล · session อัตโนมัติอ่านอังกฤษได้แม่นกว่า |
| D16 | 2026-09-02 | งานอัตโนมัติของ Claude: Phase 0–1 ทำแบบ interactive · Phase 2+ cloud routine วันละ 1 รอบสำหรับงานที่ไม่แตะ MT5 · Phase 3+ scheduled task บนเครื่องนี้สำหรับ post-mortem · ทุกอย่างเปิด PR เจ้าของ merge | ทุก 6 ชม. เร็วเกินกว่าจะรีวิวทันและ PR จะชนกัน |

| D17 | 2026-09-03 | **โบรกคือความจริง** · reconcile แยกสองทิศทางไม่เท่ากัน · **orphan** (โบรกมีไม้ที่ journal ไม่รู้จัก) = อันตราย ต้อง freeze ไม่รับไม้ใหม่ และเป็น mismatch ส่งให้ kill switch · **ghost** (journal คิดว่าเปิดอยู่ แต่โบรกบอกปิดแล้ว) = ปกติ เพราะ SL/TP ทำงาน ให้บันทึกเป็น `CLOSED_ELSEWHERE` แล้วเทรดต่อ ห้ามเตือน | ถ้าเตือนทั้งสองทาง ระบบจะร้องทุกครั้งที่ SL ทำงาน แล้วคนจะปิดระบบเตือนทิ้งภายในสัปดาห์เดียว · เสียงเตือนต้องหายากพอที่จะมีความหมาย |
| D18 | 2026-09-03 | ต้นทุนจริงที่วัดได้: **ไม้ 0.01 lot EURUSD ไป-กลับบน XM เสีย 0.20 USD** (spread 20 จุด) · ตัวเลขนี้ต้องเข้า cost model ของ backtest (P2-02) | บัญชี MetaQuotes-Demo ให้ spread 0 จุดซึ่งไม่มีจริง · ถ้า backtest ไม่คิดต้นทุนนี้ ผลย้อนหลังจะสวยเกินจริงและ edge ปลอมจะผ่าน gate ไปได้ |

| D19 | 2026-09-03 | บัญชีนี้ใช้กับ **บอทอย่างเดียว** ไม่เทรดมือปนบนบัญชีเดียวกัน · ดังนั้น kill switch **ปิดทุกโพซิชันในบัญชี** ไม่กรองด้วย magic number | เจ้าของยืนยัน 2026-09-03 · การปกป้องเงินทุนสำคัญกว่าการรักษาไม้ที่เปิดด้วยมือ และเมื่อไม่มีไม้มือก็ไม่มีอะไรให้เสีย · ถ้าวันหนึ่งจะเทรดมือบนบัญชีนี้ ต้องกลับมาแก้ข้อนี้ก่อนแก้โค้ด · `Reconciler` ยังแยก ours/foreign ได้อยู่ ใช้รายงานอย่างเดียว |
| D20 | 2026-09-03 | **วันเทรด นับตามเวลา server ของโบรก ไม่ใช่ UTC และไม่ใช่เวลาไทย** · daily loss limit รีเซ็ตตอนเที่ยงคืนของ server | ใบแจ้งยอดและ swap ของโบรกตัดตามวันของ server ถ้านับคนละวันกับโบรก ตัวเลขขาดทุนรายวันจะไม่ตรงกับที่เห็นในบัญชี |
| D21 | 2026-09-03 | **peak equity และ day-start equity ต้องอยู่รอดข้ามการรีสตาร์ท** เก็บในตาราง `state` ของ journal | ถ้ารีสตาร์ทแล้ว peak รีเซ็ต ลิมิต drawdown 30% จะวัดจากยอดปัจจุบันแทนยอดสูงสุดจริง แปลว่าปิดโปรแกรมแล้วเปิดใหม่กลายเป็นการล้างประวัติ drawdown ซึ่งทำให้ลิมิตไร้ความหมาย |
| D22 | 2026-09-03 | **Watchdog**: ทุก tick ถาม `terminal_info().connected` ของ MT5 ไม่ใช่ flag ของเราเอง ถ้าหลุดให้ shutdown แล้ว initialize ใหม่เองทันที และวัด server offset ใหม่ทุกครั้งที่ต่อกลับ · ตอน connect **retry เฉพาะ error -10003/-10005 (terminal ยังไม่พร้อม)** เท่านั้น รหัสอื่นล้มครั้งเดียวจบ · tick ที่ต่อไม่ได้ห้ามเปิดไม้ใหม่ และปล่อยให้ trigger `broker_silence` ทำงานตามปกติ | MT5 ถือ process ที่ยังมีชีวิตอยู่ได้ทั้งที่หลุดจากโบรกไปแล้ว flag ฝั่งเราจะบอกว่าปกติ มีแต่ terminal เท่านั้นที่รู้ · รหัสอื่นเช่น -6 (login ผิด/server ผิด) retry ไปก็ล้มเหมือนเดิม และ login ซ้ำ ๆ คือวิธีที่บัญชีโดนล็อก · reconnect อาจข้ามช่วงเปลี่ยน DST ถ้าไม่วัด offset ใหม่ ทุกแท่งจะเพี้ยนเงียบ ๆ (D13) · ถ้าต่อกลับไม่ได้จริง ๆ ระบบต้องหยุดเทรดแล้วให้ kill switch ตัดสิน ไม่ใช่เดาจาก position list เก่า |
| D23 | 2026-09-03 | ลิมิตชุดที่สองของ Risk Engine (P1-04b) · **free margin**: ออร์เดอร์เดียวใช้ได้ไม่เกิน **25% ของ margin ที่ว่าง** ถามราคาจาก `order_calc_margin` ของโบรกก่อน ถ้าโบรกไม่ตอบค่อยคำนวณเอง และถ้าคำนวณไม่ได้ (บัญชี USD เทรด EURGBP) ให้ **ข้ามการตรวจ ไม่ใช่ปฏิเสธ** · **correlation**: นับว่าไม้ใหม่คือ 1.0 copy แล้วบวกไม้ที่เปิดอยู่ตามค่า correlation ห้ามเกิน **2.0 copies** ตารางค่าอยู่ใน `risk/correlation.py` เป็นค่าหยาบ ๆ ใช้ปฏิเสธได้อย่างเดียว ห้ามใช้เพิ่มขนาดไม้ คู่ที่ไม่มีในตาราง = ไม่ correlate · **งบรายกลยุทธ์**: กลยุทธ์เดียวขาดทุนได้ไม่เกิน **1.5% ของ equity ต่อวัน** และถือความเสี่ยงเปิดอยู่ได้ไม่เกิน **0.5% ของ equity** | ลิมิต v0 ทั้งหมดมองที่ระดับบัญชี ไม่มีข้อไหนเห็นสามอย่างนี้ · margin ที่ 0.25% risk บน leverage 1:1000 ไม่มีทางชนอยู่แล้ว (วัดจริงบน XM demo: 0.12 lot EURUSD ใช้ margin 13.92 USD จากที่ว่าง 9,999) แต่บัญชีจริงอาจ leverage ต่ำกว่ามาก และการให้โบรกปฏิเสธออร์เดอร์เองแย่กว่าเพราะ reject 3 ครั้งติดกันจะทริกเกอร์ kill switch · currency netting จับ EURUSD+GBPUSD ได้ แต่จับ EURUSD+GBPJPY ไม่ได้ทั้งที่วันไหนตลาด risk-off มันคือไม้เดียวกัน · บัญชีที่ยัง healthy ปิดกลยุทธ์ที่กำลังพังไม่ได้เลยถ้าไม่มีงบรายกลยุทธ์ และ 1.5% คือครึ่งหนึ่งของลิมิตรายวันทั้งบัญชี แปลว่าต้องมีสองกลยุทธ์พังพร้อมกันถึงจะปิดทั้งวัน |
| D24 | 2026-09-03 | Agent ที่เหลือของ P3-03 · **Scout ห้ามเขียนลงปฏิทินเด็ดขาด** และ prompt สั่งห้ามบอกเวลา/วันที่ ผลลัพธ์เก็บเป็น briefing ในตาราง `state` อายุ 36 ชม. แล้วส่งให้ Analyst อ่านโดยติดป้ายว่าเป็นความเห็นที่ยังไม่ตรวจสอบ และกรองเหลือเฉพาะสกุลเงินของคู่ที่เทรด · **Reviewer อธิบายอย่างเดียว ห้ามเสนอให้แก้พารามิเตอร์** ผลของมันถูกบันทึกไว้ ข้าง ๆ คำตัดสินของ classifier ที่เป็นโค้ด ไม่ใช่แทนที่ · prompt ของทั้งสองตัวห้ามมีข้อมูลบัญชี (ยอดเงิน โพซิชัน ticket) เหมือน Analyst | โมเดลตอบเวลาข่าวผิดได้ และปฏิทินคือสิ่งเดียวที่หยุดบอทตอน NFP ถ้าเชื่อเวลาที่โมเดลแต่งขึ้น จะบล็อกผิดชั่วโมงและเทรดทะลุชั่วโมงที่ควรบล็อก · สิ่งที่โมเดลทำได้ดีคือ 'สัปดาห์นี้มีอะไรน่าสนใจ' ซึ่งเป็นบริบท ไม่ใช่ความจริงที่ต้องพึ่งพา · `regime` เป็นคำตัดสินที่กฎอัตโนมัติห้ามแปะเอง (D11) โมเดลก็เดาไม่ได้ดีกว่ากัน จึงให้พูดได้แต่ทำอะไรไม่ได้เลย |
| D25 | 2026-09-03 | **พาธทุกอันผูกกับโฟลเดอร์โปรเจกต์ ไม่ใช่ cwd ของ shell** · `.env` ก็อ่านจากโฟลเดอร์โปรเจกต์ก่อนเสมอ · ตั้ง `DATA_DIR` ได้ถ้าจะย้ายข้อมูลไปไดรฟ์อื่น ซึ่งเป็นการตั้งใจ ไม่ใช่อุบัติเหตุ · `.gitignore` ต้องกันไฟล์ `.db` ในโฟลเดอร์ `data/` **ทุกที่** ไม่ใช่แค่ที่ราก | เจอของจริง 2026-09-03: core ถูกสั่งรันจากโฟลเดอร์ `ui/` (เพราะเพิ่ง `npm start` อยู่ตรงนั้น) มันขึ้นมาดูปกติทุกอย่าง แต่เขียน journal ลง `ui/data/journal-demo.db` อ่าน `.env` ไม่เจอเลยจึงใช้ค่า default ล้วน และหา `data/history.db` ไม่เจอ backtest จากหน้าจอเลยล้มด้วย `only 0 bars stored` · ครึ่งที่เงียบกว่าคืออันตรายกว่า: peak equity อยู่ใน journal เพื่อให้รีสตาร์ตแล้วประวัติ drawdown ไม่หาย (D21) ถ้า journal เดินตาม cwd แปลว่าเปิดจากโฟลเดอร์ใหม่เมื่อไหร่ peak รีเซ็ตเมื่อนั้น ลิมิต 30% จะวัดจากยอดปัจจุบันแทนยอดสูงสุดจริง · และ journal ที่หลุดไปอยู่นอก `data/` ราก commit ขึ้น repo สาธารณะได้ ทั้งที่ในนั้นมีเลขบัญชีและราคาที่ฟิลจริง |
| D26 | 2026-09-03 | **ขั้น lifecycle มีผลกับลูปจริง ไม่ใช่แค่รายงาน** · เงินจริงรับเฉพาะ `live_small` กับ `live` เท่านั้น ต่ำกว่านั้นลูปปฏิเสธไม่ลงทะเบียนให้ พร้อมเขียน CRIT · `retired` ห้ามเทรดทุกที่รวมทั้งเดโม · **เดโมยังเปิดให้ทุกขั้นรันได้** แต่ตัวที่ต่ำกว่า `forward` ต้องขึ้น WARN ทุกครั้งที่สตาร์ต | ก่อนหน้านี้ `_build_core` ลงทะเบียนกลยุทธ์ทุกตัวที่ discover เจอ ไม่ดูขั้นเลย แปลว่าบันไดที่ D3 ออกแบบไว้ไม่มีใครต้องปีน และวันที่เปลี่ยนโปรไฟล์เป็น live กลยุทธ์ขั้น research จะขึ้นบัญชีจริงทันที · ที่ไม่ปิดเดโมด้วยเพราะเดโมคือที่ที่กลยุทธ์หาหลักฐานมาผ่าน gate ถ้าห้ามรันต่ำกว่า `forward` บนเดโม ก็ไม่มีทางไปถึง `forward` ได้เลย สิ่งที่เดโมทำไม่ได้คือเงียบ ต้องบอกทุกครั้งว่ากำลังรันของที่ยังพิสูจน์ไม่ได้ |
| D27 | 2026-09-03 | **stop ขยับได้ทางเดียว คือเข้าหาราคา ห้ามถ่างออก** ตรวจที่ `risk/stops.py` ที่เดียว ไม่มีพารามิเตอร์ปิดการตรวจ · กลยุทธ์เสนอ stop ใหม่ผ่าน hook `manage()` ซึ่งเป็นทางเลือก ไม่มีก็ไม่ต้องมี · เรียกครั้งเดียวต่อแท่งที่ปิดแล้ว เท่ากับจังหวะเดียวกับที่ตัดสินใจเข้าไม้ backtest กับของจริงจึงจัดการไม้ที่จังหวะเดียวกันเป๊ะ (กฎ 04) · การขยับ stop ผ่าน `execution.py` ที่เดียวเหมือนคำสั่งเทรดอื่น (กฎ 02) · ค่าเริ่มต้นของทุกกลยุทธ์คือปิด | การบีบ stop ให้แคบลงทำได้แค่ลดสิ่งที่ไม้นั้นจะเสีย ส่วนการถ่างออกคือเปลี่ยนขาดทุนที่มีขอบเขตให้ใหญ่ขึ้น โดยฟังกลยุทธ์ที่ ณ ตอนนั้นกำลังคิดผิดเรื่องตลาดอยู่พอดี ซึ่งเป็นจังหวะที่ไม่ควรเชื่อมันที่สุด · วัดผลจริงบน ema_cross: break-even 1R ดัน win rate 28.8% → 37.2% แต่ PF ขยับแค่ 0.77 → 0.78 (ตัดไม้กำไรทิ้ง) ส่วน ATR trail 2.5× ให้ PF 0.80 และ drawdown 4.43% → 3.38% ทั้งหมดยังขาดทุน เพราะกลไกจัดการไม้ไม่ได้สร้าง edge ที่ไม่มีอยู่ |
| D28 | 2026-09-03 | ลูปทำงานเป็น **market** คือ (คู่เงิน, ไทม์เฟรม) หนึ่งคู่ · งานระดับบัญชี (อ่านพอร์ต reconcile kill switch) ทำรอบละครั้ง ส่วนงานระดับตลาด (บาร์ใหม่ จัดการไม้ที่เปิด ตัดสินใจเข้าไม้) ทำแยกทีละ market · **รายชื่อ market มาจากที่กลยุทธ์ประกาศไว้เอง** `--symbol` กับ `--tf` ใช้ตัดให้แคบลงได้เท่านั้น ขยายไม่ได้ · backtest ยังเล่นทีละ market เหมือนเดิม | ก่อนหน้านี้ `CoreConfig` มีคู่เงินเดียวไทม์เฟรมเดียว ผลคือ ema_cross (H4) กับ meanrev_m15 (M15) ลงทะเบียนทั้งคู่แต่ตัวที่ไทม์เฟรมไม่ตรงกับ `--tf` ถูกข้ามทุกแท่งอย่างเงียบ ๆ ตลอดไป · และการเทรดคู่ที่สองเขียนไม่ได้เลย ทั้งที่ Risk Engine มี currency netting กับตาราง correlation รออยู่แล้วตั้งแต่ D23 · ที่ให้ market มาจากกลยุทธ์ ไม่ใช่จาก flag เพราะกลยุทธ์รู้ว่าตัวเองถูกเขียนและทดสอบมากับคู่ไหน flag ไม่รู้ |
| D29 | 2026-09-03 | เจ้าของเปิด/ปิด market ได้จากหน้าจอ และ **เพิ่ม market ที่กลยุทธ์ไม่ได้ประกาศไว้ก็ได้ แต่ติดเงื่อนไขสองข้อ** · หนึ่ง ต้องมีข้อมูลราคาของตลาดนั้นเก็บไว้แล้ว ไม่งั้นปฏิเสธ · สอง การเพิ่ม market **เตะกลยุทธ์กลับไปขั้น `research` ทันที** · ส่วนการปิด market ไม่ต้องผ่านอะไรเลย เพราะแค่หยุดตัดสินใจใหม่ ไม้ที่เปิดอยู่ยังมี SL ที่โบรกและ reconcile ยังดูอยู่ · เก็บใน `state` ของ journal ลูปอ่านใหม่ตามจังหวะ reconcile ไม่ต้องรีสตาร์ต | D28 บอกว่า market มาจากที่กลยุทธ์ประกาศ เพราะกลยุทธ์รู้ว่าตัวเองถูกทดสอบมากับคู่ไหน ข้อนี้เปิดทางให้เจ้าของสั่งเป็นอย่างอื่นได้ โดยไม่ทำให้บันได lifecycle กลายเป็นข้อเสนอแนะ · ตลาดที่ backtest ไม่ได้ก็ไต่บันไดไม่ได้ การบังคับให้มีข้อมูลก่อนคือสิ่งที่แยก 'เพิ่มคู่เทรด' ออกจาก 'เดาสุ่มคู่ใหม่' · และการผ่าน gate บน EURUSD H4 ไม่ได้พิสูจน์อะไรเลยเกี่ยวกับ GBPUSD ดังนั้นต้องกลับไปขั้น research ซึ่งตาม D26 แปลว่าแตะเงินจริงไม่ได้จนกว่าจะไต่กลับขึ้นมา · **ไม่ทำระบบเลือกกลยุทธ์ให้อัตโนมัติ** เพราะยังไม่มีกลยุทธ์ไหนผ่าน gate บนคู่ไหนเลย การให้แอปเลือกเอง = แอปเลือกของที่ไม่เคยทดสอบบนตลาดนั้นมาเทรดแทนเจ้าของ |

## ค่าตั้งต้นที่ยังไม่ล็อก (จะเป็น decision เมื่อพิสูจน์แล้ว)

- ระยะ SL/TP ของ smoke: 20 pips / 40 pips (แค่ให้ผ่าน stops level ของโบรก ไม่ใช่กลยุทธ์)
- หน้าต่าง news block ±30 นาที เฉพาะ HIGH impact
- ชั่วโมงเทรด 07:00–20:00 UTC
- งบ DeepSeek $2/วัน
